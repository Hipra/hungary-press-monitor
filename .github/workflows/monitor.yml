# Pipeline runs locally on Mac Mini via cron.
# This workflow is disabled — kept only as documentation.
name: Fetch and analyze (disabled)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled. Run locally via run.sh.'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pipeline runs on Mac Mini. See run.sh."
